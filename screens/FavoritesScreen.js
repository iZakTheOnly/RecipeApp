import { StyleSheet, Text } from "react-native";

function FavoritesScreen() {
  return <Text style={styles.text}>Dummy screen</Text>;
}

export default FavoritesScreen;

const styles = StyleSheet.create({
  text: {
    color: "white",
    textAlign: "center",
  },
});
